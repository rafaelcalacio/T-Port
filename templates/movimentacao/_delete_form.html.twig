<form method="post" action="{{ path('movimentacao_delete', {'id': movimentacao.id}) }}" onsubmit="return confirm('Deseja realmente excluir?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ movimentacao.id) }}">
    <button class="btn btn-danger btn-sm">Deletar</button>
</form>
